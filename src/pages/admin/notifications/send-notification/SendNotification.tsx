import { useEffect, useState } from 'react';
import { Mail, MessageSquare, Send } from 'lucide-react';
import { toast } from 'sonner';
import { notificationApi } from '@/services/notificationApi';
import type { Campaign, CampaignInput, NotificationChannel } from '@/types/notification.types';
import { Input } from '@/components/ui/input';
import { Button } from '@/components/ui/button';
const audiences: Array<{id:CampaignInput['audience'];label:string}>=[{id:'ALL_LEARNERS',label:'Tất cả người học'},{id:'ALL_INSTRUCTORS',label:'Tất cả giảng viên'},{id:'ALL_USERS',label:'Toàn hệ thống'},{id:'SPECIFIC_USER',label:'Một người dùng'},{id:'COURSE_STUDENTS',label:'Học viên khóa học'}];
const CampaignHistory=()=>{const[items,setItems]=useState<Campaign[]>([]);const load=()=>notificationApi.listCampaigns().then(v=>setItems(v.items)).catch(()=>undefined);useEffect(()=>{void load();const timer=window.setInterval(load,10000);return()=>window.clearInterval(timer)},[]);return <section className="rounded-3xl border bg-background p-6"><h2 className="text-lg font-bold">Lịch sử gửi</h2><div className="mt-4 space-y-3">{items.length===0?<p className="text-sm text-muted-foreground">Chưa có campaign.</p>:items.map(item=><div key={item._id} className="flex items-center justify-between rounded-xl border p-4"><div><p className="font-semibold">{item.title}</p><p className="text-xs text-muted-foreground">{item.status} · {item.stats?.requested||0} người · Email lỗi: {item.stats?.emailFailed||0}</p></div>{['PARTIAL','FAILED'].includes(item.status)&&<Button variant="outline" onClick={async()=>{await notificationApi.retryCampaign(item._id);void load()}}>Gửi lại lỗi</Button>}</div>)}</div></section>};
export const SendNotification=()=>{const[audience,setAudience]=useState<CampaignInput['audience']>('ALL_LEARNERS');const[email,setEmail]=useState('');const[courseId,setCourseId]=useState('');const[title,setTitle]=useState('');const[content,setContent]=useState('');const[channels,setChannels]=useState<NotificationChannel[]>(['EMAIL','IN_APP']);const[sending,setSending]=useState(false);const toggle=(channel:NotificationChannel)=>setChannels(v=>v.includes(channel)?v.filter(i=>i!==channel):[...v,channel]);const send=async()=>{if(!title.trim()||!content.trim()||!channels.length)return toast.error('Vui lòng nhập nội dung và chọn kênh gửi.');if(audience==='SPECIFIC_USER'&&!email)return toast.error('Vui lòng nhập email.');if(audience==='COURSE_STUDENTS'&&!courseId)return toast.error('Vui lòng nhập ID khóa học.');setSending(true);try{await notificationApi.sendCampaign({audience,specificEmail:audience==='SPECIFIC_USER'?email:undefined,courseId:audience==='COURSE_STUDENTS'?courseId:undefined,title,content,channels});toast.success('Campaign đã được đưa vào hàng đợi.');setTitle('');setContent('')}catch(error){toast.error(error instanceof Error?error.message:'Không thể tạo campaign.')}finally{setSending(false)}};return <div className="mx-auto max-w-4xl space-y-6"><div><h1 className="text-3xl font-bold">Gửi thông báo</h1><p className="text-muted-foreground">Gửi Email hoặc In-app theo nhóm người nhận.</p></div><section className="space-y-6 rounded-3xl border bg-background p-6"><div><label className="mb-2 block text-sm font-medium">Đối tượng nhận</label><div className="flex flex-wrap gap-2">{audiences.map(item=><Button key={item.id} variant={audience===item.id?'default':'outline'} onClick={()=>setAudience(item.id)}>{item.label}</Button>)}</div></div>{audience==='SPECIFIC_USER'&&<Input value={email} onChange={e=>setEmail(e.target.value)} placeholder="Email người nhận"/>}{audience==='COURSE_STUDENTS'&&<Input value={courseId} onChange={e=>setCourseId(e.target.value)} placeholder="MongoDB Course ID"/>}<div className="flex gap-4"><label className="flex gap-2"><input type="checkbox" checked={channels.includes('EMAIL')} onChange={()=>toggle('EMAIL')}/><Mail className="h-4 w-4"/>Email</label><label className="flex gap-2"><input type="checkbox" checked={channels.includes('IN_APP')} onChange={()=>toggle('IN_APP')}/><MessageSquare className="h-4 w-4"/>In-app</label></div><Input value={title} onChange={e=>setTitle(e.target.value)} placeholder="Tiêu đề"/><textarea className="min-h-36 w-full rounded-xl border bg-background p-3" value={content} onChange={e=>setContent(e.target.value)} placeholder="Nội dung; hỗ trợ {{userName}}, {{userEmail}}"/><div className="flex justify-end"><Button disabled={sending} onClick={()=>void send()}><Send className="mr-2 h-4 w-4"/>{sending?'Đang tạo...':'Gửi thông báo'}</Button></div></section><CampaignHistory/></div>};