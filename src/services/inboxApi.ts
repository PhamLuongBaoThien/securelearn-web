import apiClient from './apiClient'; import type{Ticket,TicketDetail,TicketList,TicketStatus,TicketType,ReportTargetType,TicketMessage,TicketAttachment}from'@/types/inbox.types';
type R<T>={status:'OK'|'ERR';message?:string;data:T};
const root=(admin=false)=>admin?'/api/admin/inbox':'/api/inbox';
export const inboxApi={list:async(params:Record<string,unknown>={},admin=false)=>(await apiClient.get<R<TicketList>>(`${root(admin)}/tickets`,{params})).data.data,detail:async(id:string,admin=false)=>(await apiClient.get<R<TicketDetail>>(`${root(admin)}/tickets/${id}`)).data.data,create:async(input:{type:TicketType;title:string;description:string;target?:{type:ReportTargetType;id:string;courseId?:string}})=>(await apiClient.post<R<Ticket>>('/api/inbox/tickets',input)).data.data,message:async(id:string,input:{content:string;internal?:boolean;attachmentIds?:string[]},admin=false)=>(await apiClient.post<R<TicketMessage>>(`${root(admin)}/tickets/${id}/messages`,input)).data.data,status:async(id:string,status:TicketStatus)=>(await apiClient.patch<R<Ticket>>(`/api/admin/inbox/tickets/${id}/status`,{status})).data.data,upload:async(id:string,files:File[],admin=false)=>{const form=new FormData();files.forEach(f=>form.append('files',f));return(await apiClient.post<R<TicketAttachment[]>>(`${root(admin)}/tickets/${id}/attachments`,form)).data.data},attachmentUrl:(id:string,admin=false)=>`${root(admin)}/attachments/${id}`};
